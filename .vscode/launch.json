{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "lldb",
      "name": "CMake Debug (automatic)",
      "request": "launch",
      "program": "${command:cmake.launchTargetPath}",
      "args": [],
      "cwd": "${workspaceFolder}",
    }
  ]
}